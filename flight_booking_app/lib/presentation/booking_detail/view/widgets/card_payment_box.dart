import 'package:flutter/material.dart';

class CardPaymentBox extends StatelessWidget {
  const CardPaymentBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
